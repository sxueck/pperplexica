// Settings visibility constants
export const SETTINGS_VISIBILITY = {
  HIDDEN: 'hidden',
  VISIBLE: 'visible',
} as const;

// Configuration keys
export const CONFIG_KEYS = {
  HIDE_SETTINGS: 'hideSettings',
  LIBRARY_STORAGE: 'libraryStorage',
} as const;

// Library storage options
export const LIBRARY_STORAGE_OPTIONS = {
  SQLITE: 'sqlite',
  LOCAL: 'local',
} as const; 